import React from "react";
import Popup from "../../../components/Popup";

function Register({ open, handlePopupToggle }) {
  return <Popup open={open} handlePopupToggle={handlePopupToggle}></Popup>;
}

export default Register;
